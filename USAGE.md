Paste this as one Discord message (include the ``` lines):

```
┌────────────────────────────────────┐
│ LENQ                               │
├────────────────────────────────────┤
│ MEMBERS                            │
│                                    │
│ 1. Open #queue-start               │
│ 2. Pick one or more jobs           │
│ 3. Job Hours: Max and Min          │
│                                    │
│ Max = that long or shorter         │
│     Abyss Max 12 = 12h or less     │
│ Min = that long or longer          │
│     Abyss Min 15 = 15h or more     │
│ Empty = any length                 │
│                                    │
│ Live line: #queue-dashboard        │
│ AFK = in line, no job DMs          │
│ Ready = can be offered again       │
│ Leave = confirm, then leave        │
│ /queue  /myqueue  /leavequeue      │
│                                    │
│ JOB DM — 20 seconds                │
│ Yes = on duty                      │
│ No  = keep your place              │
│ Miss twice = end of the line       │
│ Wrong hours = skipped              │
├────────────────────────────────────┤
│ STAFF                              │
│ /send-jo  jobs + offer + hours     │
│ First Ready person who matches     │
│ Several J.O.s at once              │
│ Skip who already has a DM          │
│ /queue-admin panel skip remove     │
│ complete move pause resume         │
│ open close clear                   │
│ #queue-start  #queue-dashboard     │
│   no chat, no threads              │
│ #queue-admin  #queue-logs          │
├────────────────────────────────────┤
│ ADMINS                             │
│ /setup  (safe to run again)        │
│ Bot role ABOVE Staff role          │
│ /queue-config view timezone        │
│ multiple  staff-role  edit-queue   │
└────────────────────────────────────┘
```
